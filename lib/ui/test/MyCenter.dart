import 'package:flutter/material.dart';

class Mycenter extends StatelessWidget {
  const Mycenter({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('abdennour center'));
  }
}
